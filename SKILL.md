---
name: ml-data-leakage-checker
description: ML 전처리 코드에서 데이터 누수(data leakage) 패턴을 검사한다. 입력은 파이썬 전처리/학습 코드(또는 노트북 .ipynb), 출력은 "확정위반/의심/이상없음/판정불가/전처리코드로보기어려움" 중 하나와 해당 줄 번호 + 수정 방향만이다. 과정 설명은 생략하고 결과만 짧게 출력한다. 전체 타겟 인코딩, 시계열 셔플, 피처 추가 순서, 스케일러 fit 위치, CV 방식 변경 등 누수 의심 패턴을 줄 번호 기준으로 잡아낸다.
---

# ml-data-leakage-checker

## 언제 실행하는가

- ML 전처리/학습 코드(`.py` 또는 `.ipynb`)를 작성하거나 고치고 나서 실행한다.
- 아래 중 하나라도 바꿨으면 실행한다.
  - 타겟 인코딩/or 인코딩 계열 추가·변경
  - train/test/fold 분할 방식·순서 변경
  - 피처 추가·merge/concat/transform 위치 변경
  - 스케일러/인코더/imputer 등 fit 호출 위치 변경
  - 파이프라인 밖/안에서 fit을 옮겼을 때
  - 시계열 데이터 처리 순서 변경
- "실행은 되고 점수는 좋은데 왠지 찜찜한" 코드를 빠르게 훑을 때 쓴다.

## 언제 쓰지 않는가

- 파이썬 코드가 아닌 파일을 검사하려는 경우(사용 전에 파이썬 코드인지 확인)
- 전처리/학습 패턴이 거의 없는 일반 유틸리티 코드(판정이 "전처리 코드로 보기 어려움"으로 나온다)
- 코드가 비어 있거나 파이썬 코드가 아닌 경우만 해당 파일을 바로 넣지 말고, 검사 대상 코드만 넘겨야 한다
- 원본 코드를 자동으로 고치고 싶을 때는 쓰지 않는다(이 도구는 지적만 하고 원본은 수정하지 않는다)
- 실제 데이터 흐름/객체 수명까지 정밀 추적해야 하는 경우는 이 검사만 믿지 말고 실행 환경에서 split 경계 + fit 범위를 직접 확인한다

## 실행 방법

```bash
# 파일 하나
python ./.pi/skills/ml-data-leakage-checker/check.py 전처리코드.py

# 노트북(.ipynb)
python ./.pi/skills/ml-data-leakage-checker/check.py notebook.ipynb

# 표준입력으로 붙이기
python ./.pi/skills/ml-data-leakage-checker/check.py < 전처리코드.py

# 코드 문자열
python ./.pi/skills/ml-data-leakage-checker/check.py -c "$(cat 전처리코드.py)"
```

경로는 **스킬 폴더 기준 상대경로** `.pi/skills/ml-data-leakage-checker/check.py` 로 부른다. 워크스페이스 루트로 열고 쓰면 된다.

## 여러 파일이면

파일 하나마다 따로 검사한다.

```bash
python ./.pi/skills/ml-data-leakage-checker/check.py one.py two.py
```

노트북(`.ipynb`)은 셀 코드만 순서를 살려 이어 붙여 검사한다. 셀 실행 순서가 저장 순서와 다를 수 있다는 점은 결과물 안내에서 따로 표시된다.

## 출력 형식

```
결과: 확정위반 / 의심 / 이상없음 / 판정불가 / 전처리코드로보기어려움 / 빈입력 / 파이썬아닌입력
----------------------------------------
[줄번호] 분류: 사유 한 줄.
  수정: 수정 방향 한 줄.
```

분류마다 줄 번호와 수정 방향만 표시한다. 중간 추론 과정은 생략한다.

## 판정 기준 요약

- **확정위반**: split 경계 나중에 전체 데이터 fit(scaler/encoder/imputer 등), 또는 split 이후 fit이 train만 대상으로 보이지 않는 경우. 실행 오류 없이 점수만 좋아지는 전형적 누수.
- **의심**: 시계열 데이터에서 shuffle 포함 split, split 이전에 전체 데이터 대상 피처 추가/merge/transform, fit 대상이 train 한정인지 불명확한 경우, transductive/준지도자기학습 계열 정황 등. 코드만으로 100% 확정 어려우면 의심으로 둔다.
- **이상없음**: fit이 split 이후 train 한정이고, 시계열 순서가 보존되며, 타겟이 피처 생성 단계에 섞이지 않는 등 위험 패턴이 없는 경우.
- **판정 불가**: train/test 또는 fold 분할 경계가 코드에서 확인되지 않아, 지적 대상이 split 경계 대비 어디인지 알 수 없는 경우.
- **전처리 코드로 보기 어려움**: ML 전처리/학습 패턴이 충분히 보이지 않는 경우.
- **빈 입력 / 파이썬 아닌 입력**: 빈 입력 또는 파이썬 코드로 보기 어려운 경우.

## 규칙 간단 목록

- 빈 입력, 파이썬 아닌 입력, ML 코드 아님 → 각각 사유를 안내하고 끝
- split 지점 없는 코드 → "판정 불가"로 표시
- sklearn Pipeline/ColumnTransformer를 제대로 쓴 경우 → 파이프라인 자체는 누수가 아니므로 오탐을 줄임. 단 파이프라인 밖에서 경계를 무시한 별도 fit/transform이 있으면 그 줄만 지적
- 사용자 정의 함수/클래스 안에 숨은 fit → 함수/클래스 안 fit도 추적해 지적 대상에 포함
- 노트북은 셀 순서와 실행 순서가 다를 수 있음 → 실제 실행 순서를 따로 확인하라는 안내 추가
- 코드가 여러 파일로 나뉜 경우 → 파일별로 각각 검사
- transductive 학습처럼 의도된 경우 → 누수로 확정하지 않고 "의심"으로만 표시
- 원본 코드는 수정하지 않고 지적만 함

## 주의사항

- 정적 패턴 검사이므로 실제 데이터 흐름까지 보장하지는 못한다.
- 의심 항목은 줄 번호 기준으로 의심 근거를 하나만 찍고, 최종 판단은 실행 환경에서 split 경계와 fit 범위를 직접 본다.
- 파이프라인/함수 안에 숨은 fit은 이 검사로 잡히지만, 호출 경로가 매우 복잡하면 코드 순서 기준 한계를 있다.
